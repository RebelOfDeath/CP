linked list is a linear data structure

The idea is to drop the contigius memory requiremets so thta insertions, delitions can efficiently happen at the middle also.

And there is no need to pre-alocate the space (no extra overhead)


