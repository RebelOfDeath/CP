#include<bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	/*
		In c++ maps are used to store key value pairs.

		the functions on maps are:

		insert()
		operator[]
		size()
		empty()
		clear()
		begin()
		end()
		find() 
		and many more
	*/

	//the implementation of  the  map in stl will not be discussed to the similarity
	//of it with set and unordered map()

	return 0;
}